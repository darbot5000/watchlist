<% content_for :title, "Edit #{@item.title}" %>

<div class="max-w-xl">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-white">Edit</h1>
    <p class="text-gray-400 text-sm mt-1"><%= @item.title %></p>
  </div>

  <%= render "form", item: @item %>
</div>
